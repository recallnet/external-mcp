/**
 * Shared utilities for @recallnet/external-mcp modules
 * Contains common functionality used across multiple modules.
 */

// Version information
export const utils = {
  version: "1.0.0",
};

// Export error handling utilities
export * from "./errors.js";

// Export HTML processing utilities
export * from "./html.js";

// Export shared type definitions
export * from "./types.js";

/**
 * Common types used across modules
 */
// Will be populated with shared types in Phase 3
